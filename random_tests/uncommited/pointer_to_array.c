//usage of pointer to array. Need to come again
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int (*arr)[10];//arr is a pointer to an array of 10 elements int array
	int srr[10] = {0};
	arr = &srr;
	for(i = 0; i < 10; i++)
		printf("%d", )
	return 0;
}
