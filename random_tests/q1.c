//
//
//
//
#include <stdio.h>


void push()
{
	stck
}

void pop()
{
	
}

void check(char *str)
{
	if((strlen(str) == 0) || (strlen(str)/2 !=0) )
	{
		printf("unbalanced\n");
	}
	else
	{
		push(str[1]);
	}
}

int main()
{
	char *str;
	scanf("%s", str);
	check(str);
	return 0;
}
